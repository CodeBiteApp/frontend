# Agent Rules

## Package Installation

패키지 설치는 반드시 `yarn`을 사용한다.

```bash
# 올바른 방법
yarn add <package>
yarn add -D <package>
npx expo install <package>   # Expo 호환성이 필요한 경우

# 사용 금지
npm install <package>
```
